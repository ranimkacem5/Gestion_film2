{{ form_start(form, {'attr': {'class': 'category-form'}}) }}

    <div class="form-group">
        {{ form_label(form.name, 'Nom de la catégorie', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.name, {'attr': {'class': 'form-control', 'placeholder': 'Entrez le nom de la catégorie'}}) }}
        {{ form_errors(form.name) }}
    </div>

    <div class="form-group">
        {{ form_label(form.movies, 'Associer un film', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.movies, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.movies) }}
    </div>

    <button type="submit" class="btn-submit">
        <i class="fas fa-save"></i> {{ button_label|default('Enregistrer') }}
    </button>

{{ form_end(form) }}
